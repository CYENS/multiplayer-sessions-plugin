# Add plugin as a submodule to a git repository

```bash
cd /path/to/unreal/project/
```
```bash
git submodule add git@gitlab.com:premiere-cyens/prmr-multiplayer-sessions-plugin.git Plugins/MultiplayerSessions
```